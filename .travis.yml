sudo: required
services:
  - docker
before_install:
  - docker build -t jjino/4times:v1.2 .
script:
  - echo "$docker_password" | docker login -u "$docker_username" --password-stdin
  - docker push jjino/4times:v1.2
notifications:
  email:
    - jinojoe@gmail.com
    - jjino@protonmail.com
