sudo: required

env:
  - username: jjino
    password: 56apple91$

services:
  - docker

before_install:
  - docker build -t jjino/jino-test:v1.5 .

script:
  - 'echo $(password) | docker login -u $(username) --password-stdin'
  - docker images
  - 'docker push $(username)/jino-test:v1.5'
notifications:
  email:
    recipients:
      - jinojoe@gmail.com
    on_success: always
    on_failure: always
